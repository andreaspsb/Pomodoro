# Banco de dados PostgreSQL
# No Railway, DATABASE_URL é injetada automaticamente pelo plugin PostgreSQL.
# Para desenvolvimento local, crie um banco e configure abaixo:
DATABASE_URL=postgresql://usuario:senha@localhost:5432/meu_pomodoro

# Porta do servidor (Railway injeta automaticamente)
PORT=3000

# Ambiente
NODE_ENV=development
